# 宋冬野乐迷测试 H5 视觉审计

## Design Read

Reading this as: 面向宋冬野乐迷的移动端歌曲人格 H5，采用克制的巡演票根与私人档案语言，视觉上以《安和桥北》的暗红纸面为记忆底色，以《再想想》的黑白影像和黄绿色手写为唯一高亮，保留原生 HTML、CSS、JavaScript 与现有信息架构。

- `DESIGN_VARIANCE: 6`
- `MOTION_INTENSITY: 5`
- `VISUAL_DENSITY: 5`

这是保留式重设计。问卷内容、19 首歌曲结果、songId、计分逻辑、音频映射、本地状态和票根保存流程不变。

## 基线问题

1. 首页采用通用浅色纸张模板，和两张专辑的真实视觉记忆联系较弱。
2. 首页大量垂直留白使主要按钮接近首屏底部，短屏设备的行动入口不够稳。
3. 答题进度只有一条细线，选项接近默认单选表单，确认反馈偏弱。
4. 页面切换只使用纵向淡入，不足以表达票据流程的状态推进。
5. 签发动画有纸张打印过程，但缺少签发完成的明确盖章瞬间。
6. 结果页标题、人格、专辑信息和短解读的层级接近，长歌名容易显得拥挤。
7. 播放器接近通用网页控件，没有巡演设备或磁带控制单元的质感。
8. 原 Canvas 票根与网页视觉过于同质，缩小到手机宽度后主要层级不够鲜明。

## 设计决策

- 页面锁定为暗色档案室语境。暗红作为结构和记忆底色，黄绿色只承担信号与状态。
- 全站使用方角、发丝线、穿孔线、票号和印章，不使用玻璃拟态、霓虹、紫色渐变或流媒体专辑卡片。
- 首页使用不对称椭圆暗部回应《安和桥北》的椭圆照片框，同时吸收《再想想》的黑白影像质感。
- 选项使用 A 到 D 的票面编号，确认后黄绿色整行推进，随后自动进入下一题。
- 签发状态增加票纸吐出、暗红侧标和盖章生效动画。
- 结果页将歌曲标题、歌曲人格、票根、播放器和完整解读分成明确层级。
- 播放器使用刻度轨道、黄绿色推子和暗红播放态，保留原生 range 的键盘操作能力。
- Canvas 票根继续输出 1080 x 1440 PNG，加入暗红票头、黄绿色信号线和更强的缩略图层级。

## 验证结果

- 390 x 844 全流程通过：首页、说明、10 题问卷、署名、签发、揭晓、音频播放、Canvas 生成和 PNG 下载。
- 320 x 720 超窄屏通过，无横向滚动，主要按钮可见，触控区不小于 44px。
- 最长歌名《给抱着盒子的姑娘》在 320px 结果页正常换为两行。
- Canvas 输出尺寸为 1080 x 1440，图片使用同源 data URL，不依赖音频是否加载成功。
- 数据校验为 19 个结果、19 个音频映射、白名单一致；3 个 Intro / Outro 文件仍保持排除。
- 页面保留 `viewport-fit=cover`、安全区、`playsinline`、长按保存提示和 `prefers-reduced-motion` 降级。
- 关键按钮没有文字换行，页面没有横向溢出，浏览器控制台无新增错误。

验收截图位于 `qa/screenshots/`。
